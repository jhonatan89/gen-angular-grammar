export class PaginatorModel{

    page: number;
    pageSize: number;
    totalRecords: number;

    constructor(currentPage: number, pageSize: number, totalRecords: number){
        this.page = currentPage;
        this.pageSize = pageSize;
        this.totalRecords = totalRecords;
    }
    
}